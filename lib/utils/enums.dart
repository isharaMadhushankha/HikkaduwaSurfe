enum UserRole { user, instructor }

enum BookingStatus { pending, confirmed, completed, cancelled }

enum SurfLevel { beginner, intermediate, advanced }

enum NotificationType {
  bookingRequest,
  bookingConfirmed,
  bookingCancelled,
  reviewReceived,
}
